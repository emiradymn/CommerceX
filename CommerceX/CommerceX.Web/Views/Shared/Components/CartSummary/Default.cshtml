@model Cart

<a href="/Cart" class="btn btn-sm btn-warning">Sepet</a>

@if(Model?.Items.Count() > 0)
{
<small class="text-white">
    @Model?.Items.Sum(i => i.Quantity) adet ürün.
    [@Model?.CalculateTotal()]
</small>
}